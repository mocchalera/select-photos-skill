# Implement

## Active Objective

Prepare and maintain the public `select-photos-skill` repository.

## Execution Prompt

When changing this repo:

1. Keep `select-photos/` installable as a standalone skill directory.
2. Run `make ci-check`.
3. Confirm no source photos, generated project outputs, or local absolute paths
   are tracked.
4. Update `CONTINUITY.md` and `DOCUMENTATION.md` for meaningful changes.
